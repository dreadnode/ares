Perform credential access against the target environment:
Domain: {{ domain }}
Targets: {{ target_ips | join(', ') if target_ips else 'N/A' }}
Username: {{ username or 'N/A' }}
Credential ({{ cred_type }}): {{ cred_value }}
Techniques: {{ techniques | join(', ') if techniques else 'auto-select' }}
Task ID: {{ task_id }}

Use the exact credential value above; do not substitute placeholders. Prioritize GetNPUsers/AS-REP roast, Kerberoast, secretsdump, and LSASS if credentials allow. Report any hashes or credentials using task_complete.
