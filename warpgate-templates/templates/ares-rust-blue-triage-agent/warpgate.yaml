# yaml-language-server: $schema=https://raw.githubusercontent.com/cowdogmoo/warpgate/main/schema/warpgate-template.json
metadata:
  name: ares-rust-blue-triage-agent
  version: 1.0.0
  description: Rust-native Ares blue team triage agent for initial incident assessment and alerting
  author: Dreadnode <info@dreadnode.io>
  license: MIT
  tags:
    - ares
    - rust
    - blue-team
    - defensive
    - triage
  requires:
    warpgate: ">=1.0.0"
name: ares-rust-blue-triage-agent
version: latest
base:
  image: debian:trixie-slim
  pull: true
  run_command:
    - -d
    - -i
    - -t
  environment:
    DEBIAN_FRONTEND: noninteractive
    TZ: UTC
  changes:
<<<<<<<< HEAD:warpgate-templates/templates/ares-blue-triage-agent/warpgate.yaml
    - ENTRYPOINT ["ares", "worker"]
========
    - ENTRYPOINT ["ares-worker"]
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-blue-triage-agent/warpgate.yaml
    - ENV DEBIAN_FRONTEND=noninteractive
    - ENV TZ=UTC
sources:
  - name: ares
    git:
      repository: https://github.com/dreadnode/ares.git
<<<<<<<< HEAD:warpgate-templates/templates/ares-blue-triage-agent/warpgate.yaml
      ref: main
========
      ref: feature/rust-cli
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-blue-triage-agent/warpgate.yaml
      depth: 1
      auth:
        token: ${GITHUB_TOKEN}
provisioners:
  - type: shell
    only:
      - docker.amd64
    inline:
      - apt-get update
      - apt-get install -y --no-install-recommends ca-certificates curl
      - curl -fsSL https://github.com/grafana/mcp-grafana/releases/latest/download/mcp-grafana_Linux_x86_64.tar.gz -o /tmp/mcp-grafana.tar.gz
      - tar -xzf /tmp/mcp-grafana.tar.gz -C /usr/local/bin mcp-grafana
      - chmod +x /usr/local/bin/mcp-grafana
      - rm -f /tmp/mcp-grafana.tar.gz
      - apt-get clean
      - rm -rf /var/lib/apt/lists/*
  - type: shell
    only:
      - docker.arm64
    inline:
      - apt-get update
      - apt-get install -y --no-install-recommends ca-certificates curl
      - curl -fsSL https://github.com/grafana/mcp-grafana/releases/latest/download/mcp-grafana_Linux_arm64.tar.gz -o /tmp/mcp-grafana.tar.gz
      - tar -xzf /tmp/mcp-grafana.tar.gz -C /usr/local/bin mcp-grafana
      - chmod +x /usr/local/bin/mcp-grafana
      - rm -f /tmp/mcp-grafana.tar.gz
      - apt-get clean
      - rm -rf /var/lib/apt/lists/*
  - type: file
    source: ${sources.ares}
    destination: /tmp/ares-build
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
<<<<<<<< HEAD:warpgate-templates/templates/ares-blue-triage-agent/warpgate.yaml
      - rm -f /var/lib/apt/lists/lock /var/cache/apt/archives/lock /var/lib/dpkg/lock*
      - apt-get update
      - apt-get install -y --no-install-recommends ca-certificates curl build-essential pkg-config libssl-dev
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
      - export PATH="/root/.cargo/bin:$PATH"
      - ln -sf /usr/bin/gcc /usr/local/bin/x86_64-unknown-linux-gnu-gcc
      - ln -sf /usr/bin/gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - cd /tmp/ares-build && cargo build --release --bin ares
      - cp /tmp/ares-build/target/release/ares /usr/local/bin/ares
========
      - apt-get update && apt-get install -y --reinstall --no-install-recommends libc6-dev libssl-dev pkg-config libgcc-$(gcc -dumpversion | cut -d. -f1)-dev
      - ln -sf /usr/bin/gcc /usr/local/bin/x86_64-unknown-linux-gnu-gcc
      - ln -sf /usr/bin/gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - cd /tmp/ares-build/ares-rust && cargo build --release --bin ares-worker
      - cp /tmp/ares-build/ares-rust/target/release/ares-worker /usr/local/bin/ares-worker
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-blue-triage-agent/warpgate.yaml
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
<<<<<<<< HEAD:warpgate-templates/templates/ares-blue-triage-agent/warpgate.yaml
      - rm -rf /tmp/ares-build
      - rm -rf /root/.cargo /root/.rustup /usr/local/bin/x86_64-unknown-linux-gnu-gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - rm -f /var/lib/apt/lists/lock /var/cache/apt/archives/lock /var/lib/dpkg/lock*
      - apt-get purge -y build-essential pkg-config libssl-dev || true
      - apt-get autoremove -y || true
      - apt-get clean || true
      - rm -rf /var/lib/apt/lists/*
========
      - rm -rf /tmp/ares-build /usr/local/bin/x86_64-unknown-linux-gnu-gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-blue-triage-agent/warpgate.yaml
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
      - echo "Build completed successfully"
    changes:
      - USER root
      - WORKDIR /root
targets:
  - type: container
    platforms:
      - linux/amd64
      - linux/arm64
