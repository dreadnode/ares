# yaml-language-server: $schema=https://raw.githubusercontent.com/cowdogmoo/warpgate/main/schema/warpgate-template.json
metadata:
  name: ares-rust-worker
  version: 1.0.0
<<<<<<<< HEAD:warpgate-templates/templates/ares-orchestrator/warpgate.yaml
  description: Ares orchestrator with embedded Python for LLM agent steps
========
  description: Rust-native Ares worker agent with embedded Python for LLM agent steps
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-worker/warpgate.yaml
  author: Dreadnode <info@dreadnode.io>
  license: MIT
  tags:
    - ares
    - rust
    - worker
    - agent
  requires:
    warpgate: ">=1.0.0"
name: ares-rust-worker
version: latest
base:
<<<<<<<< HEAD:warpgate-templates/templates/ares-orchestrator/warpgate.yaml
  image: debian:trixie-slim
========
  image: ghcr.io/dreadnode/ares-base:latest
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-worker/warpgate.yaml
  pull: true
  run_command:
    - -d
    - -i
    - -t
  environment:
    DEBIAN_FRONTEND: noninteractive
    TZ: UTC
  changes:
<<<<<<<< HEAD:warpgate-templates/templates/ares-orchestrator/warpgate.yaml
    - ENTRYPOINT ["ares", "orchestrator"]
========
    - ENTRYPOINT ["ares-worker"]
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-worker/warpgate.yaml
    - ENV DEBIAN_FRONTEND=noninteractive
    - ENV TZ=UTC
sources:
  - name: ares
    git:
      repository: https://github.com/dreadnode/ares.git
      ref: feature/rust-cli
      depth: 1
      auth:
        token: ${GITHUB_TOKEN}
provisioners:
  - type: file
    source: ${sources.ares}
    destination: /tmp/ares-build
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
<<<<<<<< HEAD:warpgate-templates/templates/ares-orchestrator/warpgate.yaml
      - rm -f /var/lib/apt/lists/lock /var/cache/apt/archives/lock /var/lib/dpkg/lock*
      - apt-get update
      - apt-get install -y --no-install-recommends ca-certificates curl build-essential pkg-config libssl-dev
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
      - export PATH="/root/.cargo/bin:$PATH"
      - ln -sf /usr/bin/gcc /usr/local/bin/x86_64-unknown-linux-gnu-gcc
      - ln -sf /usr/bin/gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - cd /tmp/ares-build && cargo build --release --bin ares
      - cp /tmp/ares-build/target/release/ares /usr/local/bin/ares
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
      - rm -rf /tmp/ares-build
      - rm -rf /root/.cargo /root/.rustup /usr/local/bin/x86_64-unknown-linux-gnu-gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - rm -f /var/lib/apt/lists/lock /var/cache/apt/archives/lock /var/lib/dpkg/lock*
      - apt-get purge -y build-essential pkg-config libssl-dev || true
      - apt-get autoremove -y || true
      - apt-get clean || true
      - rm -rf /var/lib/apt/lists/*
========
      - apt-get update && apt-get install -y --reinstall --no-install-recommends libc6-dev libssl-dev pkg-config libgcc-$(gcc -dumpversion | cut -d. -f1)-dev
      - ln -sf /usr/bin/gcc /usr/local/bin/x86_64-unknown-linux-gnu-gcc
      - ln -sf /usr/bin/gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
      - cd /tmp/ares-build/ares-rust && cargo build --release --bin ares-worker
      - cp /tmp/ares-build/ares-rust/target/release/ares-worker /usr/local/bin/ares-worker
>>>>>>>> 750a111 (feat: add Rust-based Ares agent templates and golden image, modernize template set):warpgate-templates/templates/ares-rust-worker/warpgate.yaml
  - type: shell
    only:
      - docker.arm64
      - docker.amd64
    inline:
      - rm -rf /tmp/ares-build /usr/local/bin/x86_64-unknown-linux-gnu-gcc /usr/local/bin/aarch64-unknown-linux-gnu-gcc
targets:
  - type: container
    platforms:
      - linux/amd64
      - linux/arm64
