## Credential Access Task: {{ task_id }}

**Technique:** {{ technique }}
**Target:** {{ target_ip }}
{% if domain %}**Domain:** {{ domain }}
{% endif -%}
{% if credential_username %}**Credential:** {{ credential_username }}@{{ credential_domain }}{% if credential_password %} / Password: {{ credential_password }}{% endif %} ({{ auth_type }})
{% endif -%}

Execute the {{ technique }} attack against the target.

{% if state_context %}
## Current Operation State

{{ state_context }}
{% endif -%}

## Output Requirements

When calling `task_complete`, provide a summary of what you found. Do NOT include credentials, hashes, users, or other structured discoveries in your result — these are automatically extracted from tool output by the parser. Just describe what happened:

```json
{
  "summary": "Brief description of what was attempted and results"
}
```
