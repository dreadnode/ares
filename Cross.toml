[target.x86_64-unknown-linux-gnu]
# Install cmake so aws-lc-sys can use its CMake builder path
# (the CC builder path fails due to GCC memcmp bug #95189)
pre-build = ["apt-get update && apt-get install -y cmake"]

[target.x86_64-unknown-linux-gnu.env]
passthrough = ["AWS_LC_SYS_CMAKE_BUILDER"]
